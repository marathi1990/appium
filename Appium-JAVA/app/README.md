# APK Directory

Place your **FluentHealth.apk** file in this directory.

The app will be referenced from this location in both local and GitHub Actions runs.

**Note:** If you want to exclude the APK from version control (recommended for large files), add this to `.gitignore`:
```
app/*.apk
```

# Maven build output
target/

# Firebase service account - NEVER commit real credentials
src/test/resources/firebase-service-account.json

# IDE files
.idea/
*.iml
.classpath
.project
.settings/

# OS files
.DS_Store
Thumbs.db

# APK files - too large for GitHub without LFS.
# app/*.apk are tracked via Git LFS (see .gitattributes) — do NOT ignore them.
*.apk
!app/*.apk
